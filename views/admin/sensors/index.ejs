<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Sensors</title>
    <%- include('../../import'); -%>
</head>

<body>
    <%- include('../../navbar'); -%>

        <div class="container">
            <div class="row">
                <h2>Gerenciamento de dispositivos controlador de detectores</h2>
            </div>

            <div class="row mb-2">
                <a href="/admin/sensors/create" class="btn btn-success">NOVO DISPOSITIVO</a>
            </div>

            <div class="row">

                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Código</th>
                            <th scope="col">Sensor</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% sensors.forEach((sensors) => { %>
                            <tr>
                                <td><%= sensors.id %></td>
                                <td><%= sensors.name %></td>
                            </tr>
                            <% }); %>

                    </tbody>
                </table>
            </div>
        </div>
</body>


</html>